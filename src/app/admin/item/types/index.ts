export interface LaundryItem {
    id: string;
    name: string;
    category: string;
    unit: string;
    status: string;
}

export interface ItemFormData {
    name: string;
    category: string;
    unit: string;
    status: string;
}
